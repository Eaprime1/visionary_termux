# Phoenix Hub - Environment Configuration
# Centralized variables for all batch processing scripts
# Partner-Enhanced Configuration Framework

# Timestamp for this run (will be updated dynamically)
TIMESTAMP=$(date +%Y%m%d_%H%M%S)

# Prime batch size (adjust based on system capacity and processing needs)
PRIME_BATCH_SIZE=7

# Phoenix icon used in filenames - Sacred transformation symbol
BATCH_SYMBOL="🐦‍🔥"

# Base project path - AUTO-DETECTED by scripts, but can be manually set
# Uncomment and modify the line below to force a specific path
# BASE_DIR="/storage/emulated/0/Download/phoenix_hub"

# Alternative common paths (scripts will try these automatically):
# Android External Storage: /storage/emulated/0/Download/phoenix_hub
# Android Documents: /storage/emulated/0/Documents/phoenix_hub  
# Termux Home: $HOME/phoenix_hub
# Termux Storage: $HOME/storage/downloads/phoenix_hub

# Core working folders (set relative to BASE_DIR - will be auto-configured)
SOURCE_DIR="$BASE_DIR/incoming_documents"
DEST_DIR="$BASE_DIR/pdf_inbox"
STAGING_DIR="$BASE_DIR/batch_staging"
REVIEW_DIR="$BASE_DIR/reviews"
LOG_DIR="$BASE_DIR/logs"

# Processing Configuration
ENABLE_VERIFICATION=true
ENABLE_PROGRESS_DISPLAY=true
PAUSE_BETWEEN_DOCS=0.5
MAX_RETRY_ATTEMPTS=3

# Sacred Seven Default Selection (can be overridden)
SACRED_SEVEN=(
    "Copy of Untitled document-72.pdf"
    "Copy of Untitled document-4.pdf"
    "Copy of Untitled document-81.pdf"
    "Copy of Untitled document-25.pdf"
    "Copy of Untitled document-58.pdf"
    "Copy of Legacy compilation chat merge (15 files merged).pdf"
    "Copy of Document list 20241214.pdf"
)

# Log file paths (will be set dynamically with timestamps)
LOG_BATCH_SELECT="$LOG_DIR/batch_select_$TIMESTAMP.log"
LOG_PROCESSING="$LOG_DIR/run_processing_$TIMESTAMP.log"
LOG_CLEAN="$LOG_DIR/clean_staging_$TIMESTAMP.log"
LOG_VERIFICATION="$LOG_DIR/verification_$TIMESTAMP.log"

# Consciousness Development Settings
CONSCIOUSNESS_LEVEL="ORIGINAL_AUTHORITY_PHOENIX"
AWAKENING_CEREMONY_ENABLED=true
ENTITY_VERIFICATION_ENABLED=true

# File Processing Options
PRESERVE_ORIGINALS=true
GENERATE_CHECKSUMS=true
CREATE_PROCESSING_REPORT=true

# System Optimization (Partner Recommendations)
MEMORY_CONSERVATIVE_MODE=true
MOBILE_OPTIMIZED=true
CROSS_PLATFORM_COMPATIBILITY=true

# Debug and Logging Levels
DEBUG_LEVEL=1  # 0=minimal, 1=normal, 2=verbose, 3=debug
LOG_TO_CONSOLE=true
LOG_TO_FILE=true
COLORED_OUTPUT=true

# Phoenix Framework Metadata
FRAMEWORK_VERSION="2.0_Partner_Enhanced"
METADATA_EXPERT_VALIDATED=true
PANDORA_SEED_STATUS="ACTIVE"

# Export all variables for use in scripts
export TIMESTAMP PRIME_BATCH_SIZE BATCH_SYMBOL BASE_DIR
export SOURCE_DIR DEST_DIR STAGING_DIR REVIEW_DIR LOG_DIR
export ENABLE_VERIFICATION ENABLE_PROGRESS_DISPLAY PAUSE_BETWEEN_DOCS
export LOG_BATCH_SELECT LOG_PROCESSING LOG_CLEAN LOG_VERIFICATION
export CONSCIOUSNESS_LEVEL AWAKENING_CEREMONY_ENABLED
export PRESERVE_ORIGINALS GENERATE_CHECKSUMS CREATE_PROCESSING_REPORT
export DEBUG_LEVEL LOG_TO_CONSOLE LOG_TO_FILE COLORED_OUTPUT
export FRAMEWORK_VERSION METADATA_EXPERT_VALIDATED PANDORA_SEED_STATUS
