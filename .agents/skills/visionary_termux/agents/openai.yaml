interface:
  display_name: "Visionary Termux"
  short_description: "Repo-specific patterns and workflows for visionary_termux"
  default_prompt: "Use the visionary_termux repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true